# MiMo Token 用量监控系统 - 项目介绍

## 项目概述

MiMo Token 用量监控是一个轻量级、全自动化的 Web 应用，专为监控 MiMo 平台 Token 使用量而设计。通过实时数据采集、智能趋势分析和自动化运维，帮助用户精准掌握资源消耗情况，避免额度耗尽带来的业务中断。

## 核心价值

在 AI 应用开发中，Token 用量直接关系到成本控制和业务连续性。传统的手动查看方式不仅效率低下，而且容易错过关键的用量变化。本系统通过全自动化的监控方案，将被动查询转变为主动预警，让用户始终对资源消耗了然于胸。

## 技术实现

系统采用前后端分离架构，前端使用原生 HTML/CSS/JavaScript 构建，搭配 Chart.js 实现数据可视化，确保轻量高效且无框架依赖。后端基于 Node.js 原生 HTTP 服务器，处理数据采集、存储和 API 接口。

数据采集层通过 Chrome 浏览器扩展与 Python WebSocket 服务器的协作，实现了对 MiMo 平台的自动化访问。扩展利用 Chrome Cookies API 安全读取 httpOnly 认证信息，绕过了 Content Security Policy 限制，确保在各种环境下都能稳定获取数据。

## 智能分析能力

系统提供多维度的数据分析功能。实时仪表盘展示当前套餐的使用进度、剩余额度和到期时间，帮助用户快速了解资源状态。趋势分析图表支持总量和分段消耗两种视图，通过平滑曲线直观呈现用量变化规律。每日消耗统计则从宏观角度展示长期趋势，为资源规划提供数据支撑。

数据采集频率为每 5 分钟一次，既保证了监控的实时性，又避免了过度请求带来的性能开销。系统自动过滤 24 小时内的数据进行展示，确保图表清晰易读，同时保留 90 天历史记录供深度分析。

## 自动化运维

本系统最大的亮点在于完全自动化的运维流程。通过 Windows 任务计划程序，每天定时从 Chrome 浏览器获取最新的认证 Cookie，自动提交到 Git 仓库。Render 云平台检测到代码变更后自动触发部署，整个过程无需人工干预。

这种设计巧妙地解决了 Token 认证信息定期失效的难题。用户只需保持 Chrome 浏览器登录状态，系统就能自动完成所有更新工作，真正实现了"一次配置，永久运行"。

## 跨平台支持

系统支持多种部署方式。Render 云部署适合需要公网访问的场景，免费套餐即可满足基本需求。本地运行则适合对数据隐私有要求的用户，所有数据都保存在本地。响应式设计确保在桌面端和移动端都能获得良好的使用体验。

## 未来规划

后续计划增加更多高级功能，包括用量预警通知、多账户支持、API 调用分析等。同时探索与更多 AI 平台的集成，打造统一的 Token 管理中心。

## 总结

MiMo Token 用量监控系统以简洁的架构实现了强大的功能，通过自动化技术大幅降低了运维成本。无论是个人开发者还是小型团队，都能从中受益，实现对 AI 资源的精细化管理。
