services:
  - type: web
    name: token-monitor
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: git pull origin main 2>/dev/null; npm start
    envVars:
      - key: MIMO_CONSOLE_COOKIES
        sync: false
